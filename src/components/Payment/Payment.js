import React from 'react';

const Payment = () => {
    return (
        <div>
            <h4 className='text-center mt-4'>Payment system's work is in progress....</h4>
        </div>
    );
};

export default Payment;